# Raptor — TODO

## Prossimi passi

- [ ] Pubblicare `raptor-docgen` su npm (`npm publish`)
  - Creare account npm se non esiste
  - `npm login` con le credenziali npm
  - `npm publish --access public` dalla cartella `raptor/`
  - Dopo la pubblicazione: `npm install -g raptor-docgen` funzionerà globalmente
